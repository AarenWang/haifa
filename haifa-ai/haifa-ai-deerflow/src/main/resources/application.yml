server:
  port: 8095

spring:
  application:
    name: haifa-ai-deerflow
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: ${OPENAI_BASE_URL:https://api.openai.com}
      chat:
        options:
          model: gpt-4o-mini

logging:
  level:
    org.wrj.haifa.ai.deerflow: DEBUG
    org.springframework.ai: DEBUG
    org.springframework.retry: DEBUG
    reactor.netty.http.client: DEBUG

haifa:
  ai:
    deerflow:
      model: ${HAIFA_DEERFLOW_MODEL:}
      max-iterations: 4
      workspace-root: ${HAIFA_DEERFLOW_WORKSPACE:${user.dir}}
      skills-root: ${HAIFA_DEERFLOW_SKILLS:${user.dir}/skills}
      skills-enabled: true
      mcp-enabled: false
      tool-search-enabled: true
      uploads-root: ${HAIFA_DEERFLOW_UPLOADS:${user.dir}/uploads}
      max-upload-bytes: 10485760
      max-converted-chars: 60000
      allowed-upload-extensions: txt,md,json,csv,log,xml,yml,yaml,properties
      system-prompt: >
        You are the Java DeerFlow lead agent prototype. Use the provided
        local tool observations when they are relevant, be concise, and name
        any files you used.
      # Research mode configuration
      max-research-steps: 20
      max-research-sources: 20
      max-fetches-per-run: 10
      research-timeout: 300000
      default-research-depth: STANDARD
      research-enabled: true
      # Research loop system prompt (when in research mode)
      research-system-prompt: >
        You are a research assistant. You can use tools to search and fetch information.
        When you need information, emit a tool call in the format: <tool_call name="tool_name">{args_json}</tool_call>
        After gathering enough evidence, provide a final answer starting with <final_answer>.
